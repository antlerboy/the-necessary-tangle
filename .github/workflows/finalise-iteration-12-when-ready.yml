name: Finalise iteration 0.12 when ready

on:
  push:
    branches: ["agent/iteration-12-practitioner-intake"]
    paths:
      - ".github/workflows/finalise-iteration-12-when-ready.yml"

permissions:
  contents: write

concurrency:
  group: finalise-iteration-12-when-ready
  cancel-in-progress: false

jobs:
  finalise:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/iteration-12-practitioner-intake
          fetch-depth: 0

      - name: Wait for an applied 0.12 release or recover it
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f scripts/apply_iteration_12.py ]; then
            echo "Durable files are not present; recovering the staged archive."
            python3 - <<'PY'
          import base64, hashlib, itertools, pathlib, re, tarfile, tempfile
          root=pathlib.Path('.iteration12-bundle')
          files=sorted(p for p in root.rglob('*') if p.is_file())
          def clean(p): return re.sub(r'[^A-Za-z0-9+/=]','',p.read_text(errors='ignore'))
          def one(parts, separate=False):
              try:
                  if separate:
                      out=bytearray()
                      for p in parts:
                          s=clean(p); s += '='*((4-len(s)%4)%4); out.extend(base64.b64decode(s,validate=False))
                      return bytes(out)
                  s=''.join(clean(p) for p in parts); s += '='*((4-len(s)%4)%4)
                  return base64.b64decode(s,validate=False)
              except Exception: return b''
          attempts=[]
          for p in files: attempts.append(([p],False))
          attempts += [(files,False),(files,True)]
          if len(files)<=8:
              for n in range(2,len(files)+1):
                  for ps in itertools.permutations(files,n): attempts += [(ps,False),(ps,True)]
          seen=set()
          for ps,separate in attempts:
              payload=one(ps,separate)
              if not payload: continue
              digest=hashlib.sha256(payload).hexdigest()
              if digest in seen: continue
              seen.add(digest)
              path=pathlib.Path(tempfile.gettempdir())/f'i12-{digest[:12]}.tgz'; path.write_bytes(payload)
              try:
                  with tarfile.open(path,'r:gz') as tf:
                      names={m.name for m in tf.getmembers()}
                      req={'scripts/apply_iteration_12.py','scripts/patch_iteration_12.py','scripts/validate_iteration_12.py'}
                      if not req.issubset(names): continue
                      for m in tf.getmembers():
                          pp=pathlib.PurePosixPath(m.name)
                          if pp.is_absolute() or '..' in pp.parts: raise SystemExit(f'unsafe path {m.name}')
                      print('Recovered',len(payload),'bytes sha256',digest,'from',[str(p) for p in ps],'separate',separate)
                      tf.extractall('.')
                      break
              except tarfile.TarError: continue
          else:
              raise SystemExit('No recoverable 0.12 archive candidate')
          PY
          fi

          if [ -f scripts/prepare_iteration_12_release.py ]; then
            python3 scripts/prepare_iteration_12_release.py
          fi
          test -f scripts/apply_iteration_12.py
          test -f scripts/patch_iteration_12.py
          test -f scripts/validate_iteration_12.py

      - name: Validate and check release contents
        run: |
          make validate
          make validate
          git diff --check
          python3 - <<'PY'
          import json
          from pathlib import Path
          d=json.loads(Path('data/public-data.json').read_text())
          labels={n.get('label','') for n in d.get('nodes',[])}
          all_text=json.dumps(d,ensure_ascii=False).lower()
          assert str(d.get('meta',{}).get('release','')).startswith('0.12-')
          assert {'Ivo Velitchkov','Patrick Hoverstadt'} <= labels
          for phrase in ('the grammar of systems','the fractal organization','patterns of strategy','survival of the viable','natural drift','explicit semantics','nodica','issues/21'):
              assert phrase in all_text, phrase
          print(json.dumps(d['meta'],indent=2,sort_keys=True))
          PY

      - name: Remove one-shot files and commit
        run: |
          rm -rf .iteration12-bundle
          rm -f .github/workflows/apply-iteration-12.yml
          rm -f .github/workflows/apply-iteration-12-v2.yml
          rm -f .github/workflows/apply-iteration-12-v3.yml
          rm -f .github/workflows/finalise-iteration-12-when-ready.yml
          rm -f scripts/prepare_iteration_12_release.py
          git config user.name "Benjamin P Taylor"
          git config user.email "ben.taylor@redquadrant.com"
          git add -A
          git diff --cached --check
          if ! git diff --cached --quiet; then
            git commit -m "Publish iteration 0.12 practitioner intake" -m "Add developed coverage of Ivo Velitchkov and Patrick Hoverstadt, incorporate the submitted viability correction, document explicit semantics and Nodica, reconcile site-generated issues with the running feedback and research queues, and provide a less forbidding route into the atlas."
            git push origin HEAD:agent/iteration-12-practitioner-intake
          fi
